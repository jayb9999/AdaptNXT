import React from 'react'
import Header from '../Header/Header'

const Returns = () => {
  const title = 'Returns'
  return (
    <div>
    <Header title={title}/>
    <div>Returns</div>
    </div>
  )
}

export default Returns